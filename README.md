# terra-cloud-infra
designed a segmented Azure network using Terraform modules with dedicated NSGs per subnet, enforcing tier-based access between web, app, and database layers following least-privilege principles
